{
  "name": "radio-web",
  "version": "1.0.0",
  "description": "A website that full of radio station from israel.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --config webpack.config.js",
    "watch": "webpack --config webpack.config.js --watch",
    "install": "install; pip3 install -r requirements.txt",
    "i-install" : "python3.12 -m venv venv && . venv/bin/activate && pip install -r requirements.txt",
    "start:api": "cd backend && unicorn server:app --reload",
    "i-start:api": "cd backend && source ../venv/bin/activate && python -m uvicorn server:app --reload"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "file-loader": "^6.2.0",
    "html-webpack-plugin": "^5.6.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "@types/node": "^20.14.9"
  }
}
